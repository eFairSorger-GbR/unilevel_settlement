.card.card-simple.margin-top{:id => "anbieter-#{provider.id}"}
  %h3.margin-top{ style: "#{'color: red' if provider.inactive?}" }= "#{provider.name} (#{provider.inactive? ? 'inaktiv' : 'aktiv'})"

  %h4 Provision
  .flex.margin-top
    .w50
      - provider.provisions.order(:level).each do |provision|
        = "#{provision.level}: #{provision.provision} €"
        %br
    .w50.t-right
      = link_to 'Bearbeiten', edit_provider_path(provider), class: 'btn-base btn-sec-ghost'